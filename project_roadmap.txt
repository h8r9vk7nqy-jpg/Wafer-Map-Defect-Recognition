================================================================================
项目名称: Wafer Map Defect Pattern Recognition (基于CNN的晶圆缺陷图识别系统)
执行人: 刘明轩 (LIU Ming Xuan)
时间: 2026年1月 - 2026年2月
目标院校: NTU / Polito / CityU / PolyU
================================================================================

[PART 1: 项目背景与数据集]
--------------------------------------------------------------------------------
1. 数据集名称: WM-811K (Wafer Map 811k Dataset)
2. 来源: 真实晶圆制造产线数据 (Real-world Fab Data)
3. 数据量: 811,457 张晶圆图
4. 核心任务: 
   从海量数据中识别出 9 类特定模式：
   - Center (中心型)
   - Donut (甜甜圈型) -> *Step 1 已验证*
   - Edge-Loc (边缘局部型)
   - Edge-Ring (边缘环型)
   - Loc (局部型)
   - Random (随机满天星)
   - Scratch (划痕型)
   - Near-full (几近全坏)
   - None (无明显图案/良品)
5. 工程意义 (用于面试): 
   通过 AI 自动识别缺陷的空间分布 (Spatial Signature)，辅助良率工程师 (YE) 快速定位工艺机台问题 (Root Cause Analysis)，从而提升良率 (Yield Enhancement)。

[PART 2: 执行步骤与进度监控]
--------------------------------------------------------------------------------

[✅ COMPLETED] STEP 1: 环境搭建与数据探查 (Data Loading)
- 目标: 配置 Python 虚拟环境，读取 .pkl 原始文件，可视化单张晶圆图。
- 关键文件: 
  - check_env.py (环境检查)
  - step1_load_data.py (读取脚本)
  - LSWMD.pkl (原始数据)
- 产出: 成功绘制出 "Donut" 类型缺陷图，验证环境无误。
- 状态: 2026-01-19 完成。

[⏳ NEXT] STEP 2: 数据清洗与预处理 (Preprocessing)
- 目标: 将非结构化的原始数据转化为模型可吃的标准格式。
- 核心动作:
  1. Filtering: 剔除无标签数据 (约80%是无标签的，我们要扔掉)。
  2. Resizing: 将不同尺寸 (26x26, 50x50...) 统一插值缩放到 64x64。
  3. Normalization: 像素归一化 (0-255 -> 0-1)。
  4. One-hot Encoding: 标签数字化 (Donut -> 1, Scratch -> 7)。
  5. Splitting: 切分训练集 (Train) 和测试集 (Test)。
- 关键文件: step2_preprocess.py
- 预期产出: processed_data.npz (保存好的压缩数据包，极大提升后续速度)。
- 常见问题: 
  - 内存溢出 (Memory Error): 数据量大，如果不先筛选直接处理可能会崩。
  - 路径错误: 找不到 .pkl 文件。

[🔒 PENDING] STEP 3: CNN 模型搭建与训练 (Model Training)
- 目标: 搭建卷积神经网络，让电脑学会看图。
- 核心架构:
  - Conv2D (提取特征: 边缘、圆环、线条)
  - MaxPooling (降维: 减小计算量)
  - Dropout (防止过拟合: 模拟泛化能力)
  - Softmax (输出分类概率)
- 关键文件: step3_train.py
- 预期产出: wafer_model.h5 (训练好的模型文件) + 训练过程Loss曲线图。

[🔒 PENDING] STEP 4: 评估与成果可视化 (Evaluation)
- 目标: 用从未见过的数据 (Test Set) 考试，生成证据链。
- 核心产出:
  1. Accuracy (准确率): 目标 > 90%。
  2. Confusion Matrix (混淆矩阵): *核心文书素材*，展示每一类别的误判情况。
- 关键文件: step4_evaluate.py

[PART 3: 个人日志与问题记录 (Log)]
--------------------------------------------------------------------------------
(在此处记录你的运行结果、报错信息、解决思路，方便后续写文书回顾)

> 2026-01-19 [Step 1 Log]:
- 环境配置成功，Python 3.14.2 (venv)。
- 成功读取 811k 数据。
- 可视化了 Index 为 7316 的 Donut 缺陷图，黄色代表缺陷，紫色代表底色。
- 感悟: 真实数据确实很乱，有的晶圆形状都不一样，必须做 Step 2 的 Resize。

> [待填入 Step 2 Log] ...

================================================================================ 